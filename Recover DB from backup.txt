#!/bin/bash

#Preconditions:

#Create database Tanya:

mysql -uroot -p -e "CREATE DATABASE IF NOT EXISTS Tanya;
	USE Tanya;
	CREATE TABLE IF NOT EXISTS Parrots (Name varchar(20), Gender char(1), Colour varchar (20));
	INSERT INTO Parrots (Name,Gender,Colour) VALUES('Kiesha', 'M', 'Green');
	INSERT INTO Parrots (Name,Gender,Colour) VALUES('Chica', 'W', 'Blue');"

echo "Preconditions:"
echo "DATABASE Tanya with TABLE Parrots was created"

#Make backup of Tanya:
#dbpass="pass"
#DATE_DIR=$(date +%Y-%m-%d-%H)
#mkdir /home/nelex/MySQL_BKP/${DATE_DIR}
#for dbname in `echo show databases| mysql -u root -p$dbpass`; do
#echo "Dump $dbname..."
#mysqldump -u root -p$dbpass $dbname > "~/MySQL_BKP/${DATE_DIR}/$dbname.sql"